# PARKING.GO BACKEND & DOCKER PRODUCTION ENVIRONMENT VARIABLES

# Server Configuration
PORT=3000
NODE_ENV=production

# Multi-Tenant PostgreSQL Database Connection
DATABASE_URL=postgresql://postgres:postgres_secure_password_2026@localhost:5432/parking_kiosk

# JWT Authentication Secret Key
JWT_SECRET=PARKING_GO_PROD_JWT_SECRET_KEY_998822

# NOTE: VietQR & SePay API Keys are NOT stored here!
# Multi-Tenant SePay Secret Keys & Bank Accounts are stored per-tenant 
# inside Database Table: `tenant_payment_configs` (column: `secret_api_key_encrypted`).
